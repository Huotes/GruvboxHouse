services:
  gruvbox-house:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: gruvbox-house
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped

  # Dev service — uncomment to use
  # gruvbox-house-dev:
  #   image: node:22-alpine
  #   working_dir: /app
  #   volumes:
  #     - .:/app
  #     - /app/node_modules
  #   ports:
  #     - "3000:3000"
  #   command: sh -c "npm install && npm run dev"
  #   environment:
  #     - NODE_ENV=development
